# Skills Navigation

The `skills/` directory is organized in two ways:

1. Executable skill packages at top-level (backward compatible imports).
2. Human-friendly category navigation under:
   - `skills/workflow/`
   - `skills/infra/`

Use category folders for discovery, then open the linked executable skill folder.
