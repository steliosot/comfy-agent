name: preview_sd15_fast_character
description: Preview a fast SD1.5 character concept image.
inputs:
  prompt:
    type: string
    default: stylized explorer character
  negative_prompt:
    type: string
    default: watermark, text, blurry, duplicate limbs, low quality
  steps:
    type: integer
    default: 12
  seed:
    type: integer
    default: 192837465
outputs:
  preview: string
requirements:
  models: []
  custom_nodes: []
  links: []
  input_modalities:
  - text_prompt
  output_modalities:
  - application/json
  model_families:
  - other
  node_count: null
  node_types: []
selection_metadata:
  family: txt2img
  resource_profile: unknown
  complexity_score: null
  estimated_runtime: depends on runtime parameters and server resources
  warnings:
  - Hand-authored skill (no embedded workflow.json); metadata is inferred and may
    be approximate.
  max_width: null
  max_height: null
  max_steps: null
