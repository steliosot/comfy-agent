# Agentic Example Assets

`example_upload_crop_download.py` expects this input filename by default:

- `redhead_portrait.png`

You can also override with:

```bash
EXAMPLE_INPUT_IMAGE=/absolute/path/to/image.png
```

Or place the file in `COMFY_INPUT_DIR` as `redhead_portrait.png`.
